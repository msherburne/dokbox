[project]
name = "dokbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "docker>=7.1.0",
    "pydantic>=2.13.4",
    "textual>=8.2.7",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
